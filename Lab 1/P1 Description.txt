// загружает ссылку из массива локальных переменных с сылками frame'а из индекса 0
// кладет эту ссылку в стек операндов
0 aload_0
// загрузка конструктора #1
1 invokespecial #1 <java/lang/Object.<init> : ()V>
4 return




// взяла #7 из констант пула (это пустая строка) и кладет на стек операндов
    0 ldc #7

// берем значение из стека операндов и сопоставляем с локальной переменной s
    2 astore_1

// кладет int на стек операндов под номером 0
// в контексте iconst цифра на конце - это число (от -1 до 5)
// команда занимает всего байт?
// кладет на operand stack
    3 iconst_0

// берем значение из стека операндов и сопоставляем с локальной переменной i
    4 istore_2

// взял этот самый (int i) и кинул в operand stack
    5 iload_2

// загружает short как int на стек операндов
    6 sipush 10000

// если (i - int) (cmp - прошла сравнение) (ge - grater or equals >=)
// берет два int'а с операнд стека и сравнивает
// сравнение знаковое
// если сравнение дало false - идем к инструкции 29
    9 if_icmpge 29 (+20)

// кладет s в стек операндов
    12 aload_1

// взял этот самый (int i) и кинул в operand stack
    13 iload_2

// взял со стека операндов i
// применил фукнцию String.valueOf(i)
// положил результат в стек операндов
    14 invokestatic #9 <java/lang/String.valueOf : (I)Ljava/lang/String;>

// вызов метода, который не известен во время компиляции
// текст с пробелом " " зашит в вызов метода при помощи BootstrapMethods #0
// итог положит в стек операндов
    17 invokedynamic #15 <makeConcatWithConstants, BootstrapMethods #0>

// берет со стека операндов значение и связывает со ссылкой s, которую кладет на стек
    22 astore_1

// i + 1
    23 iinc 2 by 1

// goto оно и в Африке goto
    26 goto 5 (-21)

// берет ссылку static поля System.out и кладет в стек операндов
    29 getstatic #19 <java/lang/System.out : Ljava/io/PrintStream;>

// кладем на стек операндов s
    32 aload_1

// берет s со стека операндов, исполняет метод, кладет int на стек операндов
    33 invokevirtual #25 <java/lang/String.length : ()I>
// берет аргумент из стека операндов, зовет метод с System.out (который лежал в стеке операндов)
// дает ему аргумент и исполняет
    36 invokevirtual #29 <java/io/PrintStream.println : (I)V>

//THE END

